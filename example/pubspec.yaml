name: example
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  fields_generator:
    path: ../

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.6
  json_serializable: ^6.9.4
